# 画像の置き場所

- 画像ファイルはこの `images/` フォルダに置いてください。
- 例: `images/zutsu.png` や `images/haraitu.jpg` など。

## 使い方

`index.html` の `PHRASE_LINES` に **5番目の項目**として画像パスを追加します。

```
頭痛|ずつう|🧠|症状|images/zutsu.png
腹痛|ふくつう|🤢|症状|images/haraitu.jpg
```

- 画像パスは `images/` から始まる相対パスでOKです。
- 画像を指定しない場合は画像を表示しません。
